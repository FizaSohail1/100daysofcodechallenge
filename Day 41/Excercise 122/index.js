// Use a while loop to count down from 10 to 1 and breaks the loop when it reaches 5.
var count = 10;
while (count > 0) {
    if (count === 5) {
        break;
    }
    console.log(count);
    count--;
}
