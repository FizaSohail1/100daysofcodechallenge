// Rounding to the Nearest Integer: Write a function that takes a decimal number as input and returns the number rounded to the nearest integer.cls


function roundnum(num:number):number{
    return Math.round(num)
}
console.log(roundnum(5.7))